/// <reference path="../scripts/typings/all.d.ts" />

((): void => {
    "use strict";

    angular
        .module('app')
        .config(config);

    config.$inject = [
    ];
    function config(): void {
    }

})();
